<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    {% # Inlined CSS Variables %}
    {% render 'css-variables' %}

    {% # Load and preload the critical CSS %}
    {{ 'critical.css' | asset_url | stylesheet_tag: preload: true }}

    {{ 'application.css' | asset_url | stylesheet_tag }}

    {% # Social, title, etc. %}
    {% render 'meta-tags' %}

    {{ content_for_header }}
  </head>

  <body>
    {% sections 'header-group' %}

    {{ content_for_layout }}

    {% sections 'footer-group' %}
    
    <script src="{{ 'product-form.js' | asset_url }}" defer></script>
    <script src="{{ 'cart-notification.js' | asset_url }}" defer></script>
    <script src="{{ 'cart-icon.js' | asset_url }}" defer></script>
    <script src="{{ 'cart-script.js' | asset_url }}" defer></script>
    <script src="{{ 'product-script.js' | asset_url }}" defer></script>
    <script src="https://unpkg.com/medium-zoom@1.0.6/dist/medium-zoom.min.js" defer></script>
    <script>
      mediumZoom('#main-product-image', {
        margin: 24,
        background: '#fff'
      });
    </script>
  </body>
</html>
