<div class="flex-1 mx-4">
    <form action="{{ routes.search_url }}" method="get" class="w-full">
      <input
        type="search"
        name="q"
        placeholder="Search products..."
        class="w-[400px] px-4 py-2 border rounded"
      />
    </form>
</div>